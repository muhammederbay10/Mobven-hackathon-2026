# YetkiCheck frontend — copy to web/.env.local and adjust locally.
# No secrets belong here: everything NEXT_PUBLIC_* is shipped to the browser.
#
# The browser talks only to the bank API. It never calls the AI service
# directly (plan section 1.3), so there is deliberately no AI_URL here.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
