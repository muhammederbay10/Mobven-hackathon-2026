# ── Secrets ──────────────────────────────────────────────────
.env

# ── Python ──────────────────────────────────────────────────
__pycache__
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# ── Virtual Environment ────────────────────────────────────
.venv/
venv/
env/

# ── IDE ─────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Testing ─────────────────────────────────────────────────
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
Data
nul
tests
deck-assets
.json
.tmp
.pytest_tmp
extraction-spike
tmp

# ── Codex ───────────────────────────────────────
AGENTS.md
CLAUDE.md
PROJECT_STATE.md

# ── Deliverables the patterns above would swallow ───────────
# "Data" (core.ignorecase=true on Windows) hides data/; "tests" hides ai/tests/.
# Both are handed to the other tracks: BE's stub mode reads ai/tests/fixtures/case{n}.json
# and FE renders data/documents/. Directory-only negations — __pycache__ stays ignored.
!ai/tests/
!data/

# ── Misc ────────────────────────────────────────────────────
*.log
ai/cache/

docs

# ---------------------------------------------------------------------------
# YetkiCheck — repository ignore rules
# See docs/fbdocs/IMPLEMENTATION_PLAN.md section 4.1 for the required layout.
# Rule of thumb: committed = reproducible demo input. Ignored = runtime state.
# ---------------------------------------------------------------------------

# --- secrets & local environment -------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key

# --- python / api -----------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# --- databases (runtime state) ---------------------------------------------
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# --- node / web -------------------------------------------------------------
node_modules/
.next/
out/
dist/
coverage/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# --- runtime data (regenerated by scripts/reset_demo.*) ---------------------
# data/registry.json is GENERATED from data/registry.seed.json at reset time.
data/registry.json
# Uploaded documents and their rendered pages are runtime artefacts.
data/uploads/*
!data/uploads/.gitkeep

# --- deliberately NOT ignored ----------------------------------------------
# data/fixtures/**            committed golden input for stub + tests
# data/documents/**           committed synthetic PDFs and page PNGs
# data/registry.seed.json     committed baseline registry
# data/cache/extractions/**   pre-warmed validated live responses; these must
#                             survive to the stage so replay works offline
#                             (IMPLEMENTATION_PLAN.md GAP-11, phases 6-8).

# --- editor / os ------------------------------------------------------------
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
